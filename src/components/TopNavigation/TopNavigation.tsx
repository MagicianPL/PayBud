import React from 'react';

import StyledWrapper from './StyledWrapper';

const TopNavigation = () => {
    return(
        <StyledWrapper>

        </StyledWrapper>
    );
};

export default TopNavigation;