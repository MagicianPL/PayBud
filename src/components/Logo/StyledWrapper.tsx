import styled from "styled-components";

const StyledWrapper = styled.div`
    width: 100%;
    max-width: 320px;

    img {
        width: 100%;
        height: auto;
    }
`;

export default StyledWrapper;